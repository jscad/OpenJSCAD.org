import Vec3 from './type'

export default rotateX

declare function rotateX(angle: number, origin: Vec3, vec: Vec3): Vec3
declare function rotateX(out: Vec3, angle: number, origin: Vec3, vec: Vec3): Vec3
