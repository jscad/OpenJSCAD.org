/** create a minkowski sum of the given shapes
 * @param {Object(s)|Array} objects either a single or multiple Shape3/CAG objects to create a hull around
 * @returns {Shape3} new Shape3 object , mirrored
 *
 * @example
 * const result = minkowski(rect(), circle())
 */
const minkowski = shapes => {
  throw new Error('minkowski() is not yet implemented')
}

module.exports = minkowski
